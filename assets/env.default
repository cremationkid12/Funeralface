# Safe defaults bundled with the app (see README). Copy `.env.example` to
# project root `.env` to override; `.env` is listed in pubspec assets.
APP_ENV=development
API_BASE_URL=http://localhost:8010
DEEPLINK_HOST=links.example.com
FAMILY_LINK_BASE=https://links.funeralface.app
SUPABASE_URL=
SUPABASE_ANON_KEY=
